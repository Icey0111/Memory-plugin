# Local scratch only. Previous revisions, audit snapshots, one-off live runs, captured chat text, raw model
# responses and credentials live in remove/ and must never reach the repository. The reusable acceptance
# runner is versioned (acceptance-longchat.mjs, acceptance-capture.js); the data it produces is not.
/remove/
